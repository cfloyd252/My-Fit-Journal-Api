CREATE TABLE exercises (
    exercise_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    user_id INTEGER
        REFERENCES users(id) ON DELETE CASCADE NOT NULL,
    exercise_name TEXT NOT NULL
);